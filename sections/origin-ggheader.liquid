{% schema %}
{
  "name": "Origin GG Header",
  "class": "origin-gucci-header-section",
  "settings": [
    {
      "type": "header",
      "content": "Logo Settings"
    },
    {
      "type": "image_picker",
      "id": "logo_image",
      "label": "Logo image",
      "info": "Upload your main logo here (transparent PNG/SVG recommended)"
    },
    {
      "type": "text",
      "id": "logo_text",
      "label": "Logo text (fallback)",
      "default": "GUCCI"
    },
    {
      "type": "range",
      "id": "hero_logo_width_desktop",
      "min": 20,
      "max": 100,
      "step": 5,
      "unit": "%",
      "label": "Huge Logo Width (Desktop)",
      "default": 60,
      "info": "Percentage of screen width"
    },
    {
      "type": "range",
      "id": "hero_logo_width_mobile",
      "min": 20,
      "max": 100,
      "step": 5,
      "unit": "%",
      "label": "Huge Logo Width (Mobile)",
      "default": 80
    },
    {
      "type": "range",
      "id": "logo_vertical_position",
      "min": 20,
      "max": 80,
      "step": 5,
      "unit": "%",
      "label": "Huge Logo Vertical Position",
      "default": 40,
      "info": "Distance from top of screen. 50% is dead center."
    },
    {
      "type": "header",
      "content": "Sticky Bar Colors"
    },
    {
      "type": "color",
      "id": "header_bg_color_shrunk",
      "label": "Background (Scrolled)",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "header_text_color_shrunk",
      "label": "Text/Icons (Scrolled)",
      "default": "#111111"
    },
    {
      "type": "header",
      "content": "Initial Colors (Overlay)"
    },
    {
      "type": "color",
      "id": "header_text_color_initial",
      "label": "Text/Icons/Huge Logo",
      "default": "#FFFFFF",
      "info": "Make sure this contrasts with your Hero Section underneath."
    },
    {
      "type": "header",
      "content": "Navigation"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu"
    },
    {
      "type": "checkbox",
      "id": "show_desktop_menu",
      "label": "Show desktop menu",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Origin GG Header"
    }
  ]
}
{% endschema %}

<style>
  /* --- VARS --- */
  .origin-gucci-header-section {
    --header-transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    --logo-width-desktop: {{ section.settings.hero_logo_width_desktop }}vw;
    --logo-width-mobile: {{ section.settings.hero_logo_width_mobile }}vw;
    --logo-vertical-pos: {{ section.settings.logo_vertical_position }}vh;
    
    --header-bg-shrunk: {{ section.settings.header_bg_color_shrunk }};
    --header-text-shrunk: {{ section.settings.header_text_color_shrunk }};
    --header-text-initial: {{ section.settings.header_text_color_initial }};
  }

  /* --- WRAPPER --- */
  /* This ensures the header sits ON TOP of the next section (the hero) */
  .origin-gucci-header {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 0; /* Minimal height so it doesn't push content down */
    z-index: 50;
  }

  /* --- STICKY TOP BAR --- */
  .origin-sticky-bar {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 100;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    padding: 16px 24px;
    box-sizing: border-box;
    transition: var(--header-transition);
    background-color: transparent; 
    color: var(--header-text-initial);
  }

  /* Scrolled State for Bar */
  .origin-gucci-header--shrink .origin-sticky-bar {
    background-color: var(--header-bg-shrunk);
    color: var(--header-text-shrunk);
    box-shadow: 0 4px 20px rgba(0,0,0,0.05);
    padding-top: 12px;
    padding-bottom: 12px;
  }

  /* --- STICKY BAR LOGO (HIDDEN INITIALLY) --- */
  .origin-sticky-bar__logo {
    opacity: 0;
    transform: translateY(10px);
    transition: var(--header-transition);
    display: flex;
    justify-content: center;
    align-items: center;
    pointer-events: none; /* Not clickable when hidden */
  }

  .origin-sticky-bar__logo a {
    display: block;
    line-height: 0;
  }

  .origin-sticky-bar__logo img {
    height: 32px; /* Small standard logo size */
    width: auto;
    display: block;
  }
  
  .origin-sticky-bar__logo span {
    font-size: 20px;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  /* Scrolled State for Sticky Logo */
  .origin-gucci-header--shrink .origin-sticky-bar__logo {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  /* --- HUGE LOGO OVERLAY --- */
  .origin-huge-logo-wrapper {
    position: fixed; /* Fixed so it stays on screen */
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    z-index: 40; /* Below sticky bar, above page content */
    pointer-events: none; /* CRITICAL: Allows clicks to pass through to the Hero Banner below */
    display: flex;
    align-items: flex-start; /* Align top, then push down with padding/margin */
    justify-content: center;
    transition: opacity 0.5s ease, transform 0.8s cubic-bezier(0.25, 1, 0.5, 1);
    transform-origin: center top;
  }

  .origin-huge-logo-inner {
    margin-top: var(--logo-vertical-pos); /* Position defined by schema */
    transform: translateY(-50%); /* Center strictly on that point */
    width: var(--logo-width-desktop);
    max-width: 1200px;
    text-align: center;
    color: var(--header-text-initial);
  }

  .origin-huge-logo-img {
    width: 100%;
    height: auto;
    display: block;
  }

  .origin-huge-logo-text {
    font-size: 10vw;
    line-height: 1;
    font-family: serif;
    text-transform: uppercase;
    margin: 0;
  }

  /* Scrolled State for Huge Logo: Shrink and vanish */
  .origin-gucci-header--shrink .origin-huge-logo-wrapper {
    opacity: 0;
    transform: scale(0.6) translateY(-50px);
  }

  /* --- ICONS & MENU --- */
  .origin-sticky-bar__menu, 
  .origin-sticky-bar__actions {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .origin-sticky-bar__actions {
    justify-content: flex-end;
  }

  .origin-sticky-button,
  .origin-icon-button {
    background: transparent;
    border: none;
    padding: 8px;
    cursor: pointer;
    color: inherit; 
    display: inline-flex;
    align-items: center;
    justify-content: center;
    transition: var(--header-transition);
  }

  .origin-icon-button svg,
  .origin-sticky-button svg {
    width: 24px;
    height: 24px;
  }

  .origin-desktop-menu {
    display: none;
    gap: 20px;
  }

  .origin-desktop-menu__link {
    text-decoration: none;
    color: inherit;
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-weight: 500;
  }

  /* --- MOBILE STYLES --- */
  @media (min-width: 990px) {
    .origin-desktop-menu {
      display: {% if section.settings.show_desktop_menu %}flex{% else %}none{% endif %};
    }
  }

  @media (max-width: 749px) {
    .origin-huge-logo-inner {
      width: var(--logo-width-mobile);
    }
    .origin-sticky-bar {
      padding: 12px 16px;
    }
  }

  /* --- MENU OVERLAY (MOBILE) --- */
  .origin-menu-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.4);
    backdrop-filter: blur(4px);
    z-index: 150;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
    display: flex;
    justify-content: flex-start;
  }

  .origin-menu-overlay.is-open {
    opacity: 1;
    pointer-events: all;
  }

  .origin-menu-drawer {
    background: #fff;
    width: 320px;
    max-width: 90vw;
    height: 100%;
    padding: 30px;
    transform: translateX(-100%);
    transition: transform 0.4s cubic-bezier(0.25, 1, 0.5, 1);
    display: flex;
    flex-direction: column;
  }

  .origin-menu-overlay.is-open .origin-menu-drawer {
    transform: translateX(0);
  }

  .origin-menu-close {
    align-self: flex-end;
    background: none;
    border: none;
    cursor: pointer;
    margin-bottom: 30px;
  }
  
  .origin-menu-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .origin-menu-list a {
    text-decoration: none;
    color: #000;
    font-size: 18px;
    font-weight: 500;
    text-transform: uppercase;
  }
</style>

<div class="origin-gucci-header" data-section-id="{{ section.id }}">
  
  <!-- 1. STICKY BAR (Nav) -->
  <header class="origin-sticky-bar">
    <div class="origin-sticky-bar__menu">
      <button class="origin-sticky-button" type="button" aria-label="Menu" id="origin-menu-trigger">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M3 18h18v-2H3v2zm0-5h18v-2H3v2zm0-7v2h18V6H3z"/></svg>
      </button>
      {% if section.settings.show_desktop_menu and section.settings.menu != blank %}
        <nav class="origin-desktop-menu">
          {% for link in linklists[section.settings.menu].links %}
            <a href="{{ link.url }}" class="origin-desktop-menu__link">{{ link.title }}</a>
          {% endfor %}
        </nav>
      {% endif %}
    </div>

    <!-- Small Logo (Hidden initially, appears on scroll) -->
    <div class="origin-sticky-bar__logo">
      <a href="{{ routes.root_url }}">
        {% if section.settings.logo_image %}
          <img src="{{ section.settings.logo_image | image_url: width: 300 }}" 
               alt="{{ shop.name }}" 
               width="{{ section.settings.logo_image.width }}" 
               height="{{ section.settings.logo_image.height }}">
        {% else %}
          <span>{{ section.settings.logo_text }}</span>
        {% endif %}
      </a>
    </div>

    <div class="origin-sticky-bar__actions">
      <button class="origin-icon-button" aria-label="Search">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><path d="M21 21l-4.35-4.35"/></svg>
      </button>
      <a href="{{ routes.cart_url }}" class="origin-icon-button" aria-label="Cart">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 20a1 1 0 1 0 0 2 1 1 0 0 0 0-2zm7 0a1 1 0 1 0 0 2 1 1 0 0 0 0-2zm-7-2h7a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2h-7a2 2 0 0 0-2 2v7a2 2 0 0 0 2 2zM6 5H4V3h2l.6 3h13.8l.6-3H23v2h-2"/><path d="M6 5l1.5 10h11L20 5"/></svg>
      </a>
    </div>
  </header>

  <!-- 2. HUGE LOGO OVERLAY (Floats over the next section) -->
  <div class="origin-huge-logo-wrapper">
    <div class="origin-huge-logo-inner">
      {% if section.settings.logo_image %}
        <img src="{{ section.settings.logo_image | image_url: width: 1600 }}" 
             alt="{{ section.settings.logo_text | escape }}" 
             class="origin-huge-logo-img"
             loading="eager"
             width="{{ section.settings.logo_image.width }}"
             height="{{ section.settings.logo_image.height }}">
      {% else %}
        <h1 class="origin-huge-logo-text">{{ section.settings.logo_text }}</h1>
      {% endif %}
    </div>
  </div>

  <!-- 3. MOBILE MENU DRAWER -->
  <div class="origin-menu-overlay" id="origin-menu-overlay">
    <div class="origin-menu-drawer">
      <button class="origin-menu-close" id="origin-menu-close" aria-label="Close">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 6L6 18M6 6l12 12"/></svg>
      </button>
      <nav>
        <ul class="origin-menu-list">
          {% for link in linklists[section.settings.menu].links %}
            <li><a href="{{ link.url }}">{{ link.title }}</a></li>
          {% endfor %}
        </ul>
      </nav>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const section = document.querySelector('.origin-gucci-header');
    if(!section) return;

    // Scroll Effect
    const handleScroll = () => {
      const scrollY = window.scrollY;
      // Trigger effect after 50px of scroll
      if (scrollY > 50) {
        section.classList.add('origin-gucci-header--shrink');
      } else {
        section.classList.remove('origin-gucci-header--shrink');
      }
    };

    window.addEventListener('scroll', handleScroll, { passive: true });
    // Initial check in case of reload
    handleScroll();

    // Menu Logic
    const menuTrigger = document.getElementById('origin-menu-trigger');
    const menuClose = document.getElementById('origin-menu-close');
    const menuOverlay = document.getElementById('origin-menu-overlay');

    const toggleMenu = (open) => {
      if(open) {
        menuOverlay.classList.add('is-open');
        document.body.style.overflow = 'hidden'; // Lock scroll
      } else {
        menuOverlay.classList.remove('is-open');
        document.body.style.overflow = ''; // Unlock scroll
      }
    };

    if(menuTrigger) menuTrigger.addEventListener('click', () => toggleMenu(true));
    if(menuClose) menuClose.addEventListener('click', () => toggleMenu(false));
    if(menuOverlay) menuOverlay.addEventListener('click', (e) => {
      if(e.target === menuOverlay) toggleMenu(false);
    });
  });
</script>


